apiVersion: apps/v1
kind: Deployment
metadata:
  name: addbook
  labels:
     app: addbook
spec:
  replicas: 2
  selector:
    matchLabels:
      app: addbook
  template:
    metadata:
      labels:
        app: addbook
    spec:
      imagePullSecrets:
      - name: my-registry-key
      containers:
      - name: addbook
        image: 858990550959.dkr.ecr.ap-south-1.amazonaws.com/repo1:34
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: addbook
  labels: 
      app: addbook
spec:
  selector:
    app: addbook
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: LoadBalancer
