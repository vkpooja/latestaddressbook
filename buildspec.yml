version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - echo nothing to do in the pre-build phae...
  build:
    commands:
      - echo build started on 'date'
  post_build:
    commands:
      - echo build completed on 'date'
artifacts:
  files:
    - target/addressbook.war
    - appspec.yml
    - 'deploy-scripts/**/*'
  discard-paths: yes